{
  "name": "trustlens",
  "version": "0.1.0",
  "description": "End-to-end AI platform for manipulation, bias & deception detection",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/trustlens/trustlens.git"
  },
  "author": "TrustLens Team",
  "license": "MIT",
  "keywords": [
    "ai",
    "manipulation-detection",
    "bias-detection",
    "explainability",
    "media-literacy"
  ]
}