{
  "name": "@trustlens/sdk-py",
  "version": "0.1.0",
  "description": "Python client SDK package wrapper",
  "private": true,
  "scripts": {
    "build": "poetry build",
    "dev": "poetry install",
    "test": "poetry run pytest",
    "lint": "poetry run black . && poetry run isort .",
    "typecheck": "poetry run mypy .",
    "clean": "rm -rf dist/ .pytest_cache/ __pycache__/"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}